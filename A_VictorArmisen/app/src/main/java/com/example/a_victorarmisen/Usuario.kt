package com.example.a_victorarmisen

data public class UserMode (val id :String? = null,
                     val username: String? = null,
                     val email: String? = null) {

}

/*
val userMode = UserModel(id = authResult.user?.uid "",
FirebaseFirestore.getInstance().collection("Users").add(userModel)
.document(id = authResult.user?.uid "")
 */

